<!DOCTYPE html>
<html>
<head>
    <title>All Products</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>All Products</h1>
    <ul>
        <% products.forEach(p => { %>
            <li>
                <a href="/products/<%= p.id %>"><%= p.name %></a> - $<%= p.price %>
            </li>
        <% }) %>
    </ul>
</body>
</html>
